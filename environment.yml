# conda env create --file environment.yml
# mamba env create --file environment.yml
name: profetional_ml_sklearn
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - black
  - invoke
  - jupyter
  - jupyterlab
  - matplotlib
  - nbdime
  - numpy
  - pandas
  - pip
  - plotly
  - python=3.6
  - python-dotenv
  - scikit-learn
  - scipy
  - joblib
  - pandas